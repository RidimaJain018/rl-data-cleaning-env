# Core data
pandas>=2.0
numpy>=1.24

# Environment server
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
python-multipart>=0.0.9   # required for FastAPI file uploads (/upload endpoint)

# Client
httpx>=0.27

# UI (Gradio demo)
gradio>=4.0

# LLM agent (optional — needed for inference.py --agent llm)
openai>=1.0
